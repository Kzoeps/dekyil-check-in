import * as React from 'react';
import {Typography} from '@mui/material';

export const Dashboard = () => {
  return (
    <div>
      <Typography variant="h3">Dashboard</Typographyv>
    </div>
  );
};

export default Dashboard;
